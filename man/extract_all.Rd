% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_all}
\alias{extract_all}
\title{Run the full source data extraction pipeline.}
\usage{
extract_all(
  dataset_name,
  db,
  domains = c("conditions", "procedures", "drugs"),
  cohort_id = NULL,
  cohort_schema = NULL,
  save = TRUE
)
}
\arguments{
\item{dataset_name}{Short label for the dataset (e.g. "EH30", "EstBB").}

\item{db}{Either a DuckDB file path (string) or an existing connection
list from \code{syrona_connect} / \code{syrona_connect_pg}.}

\item{domains}{Character vector of domains to extract.
Options: \code{"conditions"}, \code{"procedures"}, \code{"drugs"}.}

\item{cohort_id}{Integer \code{cohort_definition_id} to filter by.
If \code{NULL} (default), extracts the full dataset.}

\item{cohort_schema}{Schema containing the cohort table.}

\item{save}{If \code{TRUE} (default), saves CSV to \code{data/sources/<dataset_name>/}.}
}
\value{
Named list of tibbles matching the Syrona schema (invisible).
}
\description{
Extracts stratified prevalence tables from an OMOP CDM database for one or
more clinical domains. Applies k-anonymity suppression and optionally saves
results to CSV.
}
